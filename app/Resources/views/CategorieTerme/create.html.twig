{% extends 'base.html.twig' %}

{% block body %}
	<div class="row">
		<h1>Créer une catégorie Terme Commercial</h1>
		<p class="lead">Vous etes sur le point de créer une catégorie de terme commercial</p>
					<a type="button" href="{{ url('CategorieTerme_list')}}" class="btn btn-default">Annuler</a>
	</div>
	<div class="row">
		<div class="col-lg-12">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}  	
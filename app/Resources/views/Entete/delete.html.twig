{% extends 'base.html.twig' %}

{% block body %}

	<h1>Effacer une Entete</h1>
	<p class="lead">Consulter et editer les Entetes utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des Entetes</p>
	<hr>

	<h2 class="page-header">{{ nom }}</h2>
	<table>
		<tbody>
			<tr>
				<td>
					<a type="button" href="{{ url('Entete_delete_confirmed', {'id' : id})}}" class="btn btn-danger">Effacer {{ nom }}</a>
				</td>
				<td>
				-
				</td>
				<td>
					<a type="button" href="{{ url('Entete_list')}}" class="btn btn-default">Annuler</a>
				</td>
			</tr>
		</tbody>
	</table>

{% endblock %}
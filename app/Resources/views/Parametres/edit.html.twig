{% extends 'base.html.twig' %}

{% block body %}


<h1>Editer un Parametre global</h1>
	<p class="lead">Consulter et editer les Parametres global utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des Parametres global</p>
<hr>
<a type="button" href="{{ url('Parametres_list')}}" class="btn btn-default">Retour à la liste</a>
<h2 class="page-header">Modification de {{ nom }}</h2>
	<div class="row">
		<div class="col-lg-12">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
	</div>

{% endblock %}
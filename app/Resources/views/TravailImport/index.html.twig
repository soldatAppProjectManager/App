{% extends 'base.html.twig' %}

{% block body %}
<ul class="nav nav-tabs"> 
  <li >
    <a href="{{ url('TravailAvantVente_list')}}">Avant-Vente</a>
  </li> 
  <li >
    <a href="{{ url('TravailCommercial_list')}}">Vente</a>
  </li> 
  <li class="active">
    <a href="{{ url('TravailImport_list')}}">Import</a>
  </li> 
  <li >
    <a href="{{ url('TravailLivraison_list')}}">Livraison</a>
  </li>
</ul>	<h1>Liste des TravailImport</h1>
	<p class="lead">Consulter et editer les TravailImport utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des TravailImport</p>

	<div class="row">
		<div class="col-lg-12">
			<a type="button" href="{{ url('Travail')}}" class="btn btn-default">Retour aux travaux</a>
			<hr>
			<table class="table table-hover">
				<thead>
					<tr>
						<th>#</th>
						<th>Nom</th>
						<th>Description</th>
						<th>Charge</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					{% for TravailImport in list %}
						<tr>
							<th scope="row">{{ TravailImport.id }} </th>
							<td>{{ TravailImport.nom }} </td>
							<td class="col-lg-6">{{ TravailImport.description }} </td>
							<td>{{TravailImport.charge  * 100 | number_format(2)}} % </td>
							<td>
								<a type="button" class="btn btn-default" href="{{ url('TravailImport_voir', {'id' : TravailImport.id})}}">Voir</a>
								<a type="button" class="btn btn-warning" href="{{ url('TravailImport_edit', {'id' : TravailImport.id})}}">Editer</a>
								<a type="button" class="btn btn-danger" href="{{ url('TravailImport_delete', {'id' : TravailImport.id})}}">Effacer</a>
							</td>
							</tr>
					{% endfor %}
				</tbody>
			</table>

				<hr>
				<a type="button" class="btn btn-success" href="{{ url('TravailImport_create')}}">Ajouter un TravailImport</a>

			      

		</div>
	</div>
{% endblock %}  
{% extends 'base.html.twig' %}

{% block body %}
	<ul class="nav nav-tabs"> 
  <li >
    <a href="{{ url('TravailAvantVente_list')}}">Avant-Vente</a>
  </li> 
  <li >
    <a href="{{ url('TravailCommercial_list')}}">Vente</a>
  </li> 
  <li >
    <a href="{{ url('TravailImport_list')}}">Import</a>
  </li> 
  <li class="active">
    <a href="{{ url('TravailLivraison_list')}}">Livraison</a>
  </li> 
</ul>
	<h1>Effacer un TravailLivraison</h1>
	<p class="lead">Consulter et editer les TravailLivraison utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des TravailLivraison</p>

	<hr>

	<h2 class="page-header">{{ nom }}</h2>
	<table>
		<tbody>
			<tr>
				<td>
					<a type="button" href="{{ url('TravailLivraison_delete_confirmed', {'id' : id})}}" class="btn btn-danger">Effacer {{ nom }}</a>
				</td>
				<td>
				-
				</td>
				<td>
					<a type="button" href="{{ url('TravailLivraison_list')}}" class="btn btn-default">Annuler</a>
				</td>
			</tr>
		</tbody>
	</table>

{% endblock %}
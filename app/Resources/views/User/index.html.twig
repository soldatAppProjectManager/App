{% extends 'base.html.twig' %}

{% block body %}
<div class="row" >
	<h1>Liste des User</h1>
	<p class="lead">Consulter et editer les User utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des contact.</p>

	<div class="row">
		<div class="col-lg-12">
			<hr>
			<table class="table table-hover">
				<thead>
					<tr>
						<th>#</th>
						<th>Utilisateur</th>
						<th>Login</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					{% for User in Users %}
						<tr>
							<th scope="row">{{ User.id }} </th>
							<td>{{ User.civilite }} {{ User.prenom|capitalize }} {{ User.nom|capitalize }}</td>
							<td>{{ User.login }} </td>
							<td>
								<a type="button" class="btn btn-default" href="{{ url('User_voir',{'id' : User.id})}}">Voir</a>
								<a type="button" class="btn btn-primary" href="{{ url('User_edituserinfo',{'id' : User.id})}}">Editer compte</a>
								<a type="button" class="btn btn-warning" href="{{ url('User_edit',{'id' : User.id})}}">Editer profil</a>
								<a type="button" class="btn btn-danger" href="{{ url('User_delete',{'id' : User.id})}}">Effacer</a>
							</td>
							</tr>
					{% endfor %}
				</tbody>
			</table>
				<hr>
				<a type="button" class="btn btn-success" href="{{ url('User_create')}}"><i class="fa fa-user-plus" aria-hidden="true"></i> Ajouter un User</a>		
		</div>
	</div>
</div>
{% endblock %}  
{% extends 'base.html.twig' %}
{% block title %} Effacer {{ contact}} {% endblock %}
{% block body %}

	<h1>Effacer un contact</h1>
	<p class="lead">Consulter et editer les contact utilisées dans la gestion commerciale.<br> A venir, une mise a jour automatique des contact.</p>
	<hr>

	<h2 class="page-header">{{ contact }}</h2>
	<table>
		<tbody>
			<tr>
				<td>
					<a type="button" href="{{ url('contact_delete_confirmed', {'source' : source,'id' : contact.id}) }}" class="btn btn-danger">Effacer {{ contact }}</a>
				</td>
				<td>
				-
				</td>
				<td>
				{% if source == 'Fournisseur' %}
					<a type="button" href="{{ url('fournisseur_voir', {'id' : contact.fournisseur.id})}}" class="btn btn-default"><i class="fa fa-undo" aria-hidden="true"></i> Annuler</a>
				{% elseif source == 'devis' %}
					<a type="button" href="{{ url('devis_create')}}" class="btn btn-default"><i class="fa fa-undo" aria-hidden="true"></i> Annuler</a>
				{% else %}
					<a type="button" href="{{ url('client_voir', {'id' : contact.client.id})}}" class="btn btn-default"><i class="fa fa-undo" aria-hidden="true"></i> Annuler</a>
				{% endif %}
				</td>
			</tr>
		</tbody>
	</table>

{% endblock %}
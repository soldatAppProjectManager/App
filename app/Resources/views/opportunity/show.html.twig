{% extends 'base.html.twig' %}

{% block body %}
    <h1>Opportunitytype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opportunityType.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ opportunityType.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('opportunity_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('opportunity_edit', { 'id': opportunityType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}


<h1>Editer un Statut Produit</h1>
	<p class="lead">Vous etes sur le point de editer un nouveau statut produit</p>
<hr>
<a type="button" href="{{ url('statutProduit_list')}}" class="btn btn-default">Retour à la liste</a>
<h2 class="page-header">Modification de {{ nom }}</h2>
	<div class="row">
		<div class="col-lg-12">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
	</div>

{% endblock %}